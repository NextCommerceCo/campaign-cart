/**
 * SDK Version Configuration
 * Update this file to change the SDK version across all documentation
 */

export const SDK_VERSION = 'v0.3.8';

export const SDK_URL = `https://cdn.jsdelivr.net/gh/NextCommerceCo/campaign-cart@${SDK_VERSION}/dist/loader.js`;

export const SDK_SCRIPT_TAG = `<script src="${SDK_URL}" type="module"></script>`;

// For code examples that need just the URL
export const SDK_CDN = `https://cdn.jsdelivr.net/gh/NextCommerceCo/campaign-cart@${SDK_VERSION}/dist/`;
