# Attributes

The campaign store has no HTML data attributes. It is a pure Zustand store — not an enhancer.

The store is configured programmatically via `loadCampaign(apiKey, options)`. See [object-attributes.md](./object-attributes.md) for the shape of state fields and [get-started.md](../get-started.md) for how initialization works.
